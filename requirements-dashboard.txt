# Dependencias solo para el Dashboard de monitoreo (ver dashboard/app.py).
# No hacen falta para correr el bot en si (run_bot.py) - por eso estan
# separadas de requirements.txt, igual que requirements-build.txt.
streamlit>=1.32.0
plotly>=5.18.0
pandas>=2.0.0
numpy>=1.24.0
